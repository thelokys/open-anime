export type CreateAnimeInput = {
  title: string;
  progress: string;
  totalEpisodes: string;
  coverImage: string;
};

export type CreateAnimeOutput = {
  id: string;
  title: string;
  progress: string;
  totalEpisodes: string;
  coverImage: string;
};
